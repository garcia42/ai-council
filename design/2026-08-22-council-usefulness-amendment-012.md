# Council usefulness preregistration amendment 012

Status: FROZEN BEFORE PROSPECTIVE V2 DATA
Timestamp: 2026-08-23T09:06:03Z
Reason: the tenth integrated implementation council reproduced restore-containment,
parser-nondisclosure, enum-type, repeated-classification, and grouping-authority defects in an exact
tree with 334 passing tests and one intentional rehearsal-only skip.
Independent review run ID: `run-a7f0fa9c6e364575a7c9b8cb8ac2ae87`.

This file appends to the preregistration and amendments 001 through 011. No V2 activation or
eligible V2 observation exists. The reviewed shared outcome remains intervention-sensitive and is
excluded from the exogenous V2 Brier headline.

## Restore destination custody

Restore pins the validated destination parent through staging creation, recursive copy, fsync,
verification, publication, and cleanup. Staging uses retained dirfds rather than mutable absolute
pathnames. Plain-directory or symlink substitution cannot redirect any private evidence byte into
the repository, a live tree, or another outside location, even when restore later fails. Cleanup
addresses the exact pinned staging inode; no substituted namespace can strand private staging
content elsewhere.

## Generic retained-artifact parse failures

Strict parsers may retain detailed internal diagnostics, but every public writer/report/CLI wrapper
around retained prompt, output, baseline, and metadata bytes emits only a fixed safe parse category.
Duplicate keys, including secret-shaped keys, are never copied into exceptions, stderr, invalid-row
details, or report JSON. Writer- and report-time regressions use caller-controlled duplicate keys.

## Typed closed enums

Every enum-like field is first validated as text before set or mapping membership. Objects, arrays,
booleans, and numbers fail with `CaptureSchemaError`, never raw `TypeError`. Tolerant reporting
keeps an identifiable malformed V2 row as one invalid denominator record, and the CLI never emits a
traceback for valid JSON of the wrong field type. This applies to kinds, roles, seat states,
dispositions, outcome classes, invalidation reasons, and every other closed enum.

## One classification per underlying outcome

All V2 issuances sharing an outcome fingerprint use the same prospective `outcomeClass`. A linked
retry cannot relabel an intervention-sensitive outcome as exogenous. Historical or corrupt mixed
classes conservatively taint the entire fingerprint as intervention-sensitive and exclude every
issuance for that fingerprint from headline Brier. Reports expose the conflict diagnostic.

## Seat findings and operator groups are separate layers

Seat-originated exact finding fields are `findingId`, `seatId`, `category`, `claim`, `severity`,
`proposedAction`, and `evidenceSummary`. The operator assigns the within-run `group` only after all
seat outputs are retained, under the frozen grouping rubric. The group is sealed in completion but
is not required to appear in independent seat outputs. Operator dispositions remain separate too.
Writer- and report-time checks prevent alteration of the seven seat-owned fields while allowing one
operator group to reconcile genuinely overlapping findings from different seats. Reports continue
to label the result `within-run finding overlap`, not statistical independence or redundancy.

Live V2 activation remains disabled by the independent-audit and off-host-durability blockers.
