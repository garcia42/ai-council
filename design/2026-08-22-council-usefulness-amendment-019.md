# Council usefulness preregistration amendment 019

Status: FROZEN BEFORE PROSPECTIVE V2 DATA
Timestamp: 2026-08-23T10:35:24Z
Reason: the thirteenth integrated implementation council approved methodology and operations but
reproduced an installer target-substitution overwrite and secret reflection from the V2 resolution
sidecar in an exact tree with 384 passing tests and one intentional rehearsal-only skip.
Independent review run ID: `run-b7d6531bec784f1b9b7ffbd2bdd5b45d`.

This file appends to the preregistration and amendments 001 through 018. No V2 activation or
eligible V2 observation exists.

## Installer publication is conditional on the checked target identity

Installing a replacement cannot report success after the destination name stops identifying the
exact inode that was checked and backed up. The mutation boundary uses a conditional or reversible
publication protocol that detects target substitution without silently overwriting the substitute.
On detection it fails closed, preserves the concurrent target bytes, and restores every installer-
owned change from verified backups. A deterministic race hook substitutes a different regular file
after the last pre-publication validation; tests require failure, no loss of the substitute, no
installed bytes at that name, and a usable backup/rollback record for installer-owned state.

## Every report JSONL input has one exact-byte secret boundary

`capture-report` secret-scans the exact immutable byte snapshot of both the primary V2 ledger and
the V2 resolution sidecar, then decodes those same bytes. A secret-shaped key, kind, identifier, or
value in either store fails with one fixed non-reflective error before lookup, integrity validation,
invalid-record diagnostics, or CLI output. Direct runtime and subprocess CLI regressions cover a
secret-shaped unrelated sidecar kind and require no stdout, no reflected bytes in stderr, and no
mutation.

Live V2 activation remains disabled by the independent-audit and off-host-durability blockers.
