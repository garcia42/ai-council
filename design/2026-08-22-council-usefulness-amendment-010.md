# Council usefulness preregistration amendment 010

Status: FROZEN BEFORE PROSPECTIVE V2 DATA
Timestamp: 2026-08-23T08:05:23Z
Reason: the eighth integrated implementation council unanimously reproduced live-path
authorization, snapshot-lock identity, and finding-support language defects in an exact tree with
306 passing tests and one intentional rehearsal-only skip.
Independent review run ID: `run-378384e42a7048f88a180485b0e1dc5a`.

This file appends to the preregistration and amendments 001 through 009. No V2 activation or
eligible V2 observation exists. The reviewed shared outcome remains intervention-sensitive and is
excluded from the exogenous V2 Brier headline.

## Resolution authority is a complete write set

Before a V2 resolution command opens any ledger or lock, the authority boundary classifies and
authorizes the capture ledger, its derived ledger lock, the resolution sidecar, its derived lock,
and the shared evidence coordination lock. Moving the sidecar and coordination lock outside the
live tree cannot authorize a non-authority host to open or create a live capture-ledger lock.
Rejection occurs before any target or derived lock is created, opened, or changed.

## Snapshots share the writer's pinned lock identity

Snapshot coordination uses the same pinned-parent, no-follow directory-entry identity boundary as
writers. The shared holder coordinates on the pinned parent directory and revalidates before and
after copying that the coordination-lock name still identifies its opened inode. Renaming or
replacing the lock path cannot allow a writer and snapshot to enter overlapping successful
critical sections. The adversarial regression requires a snapshot to be wholly before or wholly
after the ledger-plus-artifact append; a manifest-valid mixed cut is a failure.

## Evidence summaries are not support adjudication

The MVP records a seat-supplied `evidenceSummary`; it does not independently establish that a
finding is evidentially supported. Public descriptions distinguish those summaries from support
adjudication until the separately frozen independent finding-audit control is implemented. The
existing restrictions on causal value, independence, redundancy, replaceability, and seat ranking
remain unchanged.

Live V2 activation remains disabled by the independent-audit and off-host-durability blockers.
