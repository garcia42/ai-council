[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "council-tools"
version = "0.1.0"
description = "Append-only council forecast validation and descriptive Brier reporting"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
